# DebugKit Screenshots

This directory holds the official screenshots used in the DebugKit documentation and pub.dev pages.

## Screenshot Capture Checklist

Before public release, capture these exact screenshots from the Example App (`examples/debug_kit_example`). Do not fabricate them.

- [ ] `overlay-button.png` — Show the floating DebugKit button with the error badge visible.
- [ ] `console-all-logs.png` — Show the console with a mix of manual logs (info, debug, warning).
- [ ] `console-network-log.png` — Show a Dio network entry with HTTP status code and latency.
- [ ] `console-router-log.png` — Show a GoRouter navigation push or pop entry.
- [ ] `console-riverpod-log.png` — Show a Riverpod state update or failure entry.
- [ ] `console-sanitized-log.png` — Show a log with the `***` masked password.
- [ ] `console-expanded-log.png` — Show a single log tile expanded, revealing metadata, timestamps, and stack traces.

*(Please replace these placeholders with actual `.png` files when ready)*
