name: debug_kit_dio
description: "Dio network logging adapter for DebugKit. Automatically logs requests, responses, and errors with sanitization."
version: 0.2.1
repository: https://github.com/iamPedram1/debug_kit
issue_tracker: https://github.com/iamPedram1/debug_kit/issues
topics:
  - logging
  - dio
  - networking
  - devtools
  - debug_kit

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  debug_kit: ^0.5.0
  dio: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
